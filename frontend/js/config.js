/**
 * config.js
 * ---------
 * Set this to the URL of your deployed backend (Render Web Service).
 * While testing locally, leave it pointing at your local Flask server.
 *
 * IMPORTANT: After you deploy the backend on Render, copy its URL
 * (e.g. https://chemical-kinetics-backend.onrender.com) and paste it
 * below, WITHOUT a trailing slash.
 */
const API_CONFIG = {
  BASE_URL: "http://127.0.0.1:5000"
};
